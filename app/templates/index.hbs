{{page-title "Index"}}
<Input type="email" @value={{@model.email}} placeholder="Votre email"/>
<Input type="password" @value={{@model.password}} placeholder="Votre mot de passe"/>
<button {{action "login" @model}}>Connexion</button>
{{outlet}}